import React from 'react';
// TESTING
const Explore = (props) => {
  console.log('--PROPS.USER_ID from EXPLORE', props.user_id);
  return (
    <div>
      EXPLORE
    </div>
  );
};

export default Explore;
