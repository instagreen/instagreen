import React from 'react';
// import Post from './post.jsx';


const Feed = (props) => {
  return (
    <div>
      {/* after the post is ready uncomment the following line to make them render */}
      {/* {this.props.feed.map((post) => { return <Post post={post} key={post.id} />; })} */}
    </div>
  );
};


export default Feed;
